{
  "name": "helper",
  "private": true,
  "engines": {
    "node": ">=20.19.0"
  },
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "dev": "concurrently -k -n server,web -c blue,green \"npm run dev -w apps/server\" \"wait-on tcp:3001 && npm run dev -w apps/web\"",
    "build": "npm run build -w apps/server && npm run build -w apps/web",
    "start": "npm run start -w apps/server",
    "postinstall": "npm rebuild better-sqlite3"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "wait-on": "^9.0.4"
  },
  "dependencies": {
    "pg": "^8.20.0"
  }
}
